# Screenshots — SignFlow

Coloque aqui 3 prints (PNG, < 1MB cada):

- `dashboard.png` — tela principal logada
- `editor.png` — editor de documentos com assinatura
- `pricing.png` — página de planos

Como gerar:
1. Rode `npm run dev`, abra http://localhost:3000
2. Print com Win+Shift+S ou Chrome DevTools (Ctrl+Shift+P → "Capture full size screenshot")
3. Salve nesta pasta com os nomes acima e faça push — o README já referencia essas imagens.
